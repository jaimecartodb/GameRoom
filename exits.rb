module GameRoom

	class Exits

		def initialize(no=0, so=0, we=0, ea=0)
			@no = no
			@so = so
			@we = we
			@ea = ea
		end

	end

end


##conviene crear un nuevo archivo para esto? No es mejor ponerlo dentro del archivo room.rb??
