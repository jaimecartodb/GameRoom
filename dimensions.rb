module GameRoom

	class Exits

		def initialize(no, so, we, ea)
			@no = no
			@so = so
			@we = we
			@ea = ea
		end

	end

end


##conviene crear un nuevo archivo para esto? No es mejor ponerlo dentro del archivo room.rb??
